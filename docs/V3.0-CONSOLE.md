# V3.0 — the console round

Adam, 2026-09-02, verbatim — the round's whole brief. Each numbered item
becomes a pass (one commit, full gate) in this doc's own log below.

> "Ok.  Back inside the app, here are my next round to plan, flag, decide
> and then let's make some magic:
>
> Update the Download File section so that when you click it open, it
> expands to reveal the components of the file and which are present with
> the ability to download each file or the full workbrain folder.  From
> the home page through every other interview canvas and other branches
> off a main interview path with the white background to all be the faint
> animated background from the start of the splash page.  I want that to
> remain consistent across the experience so it draws you back and
> orients you to the interview canvas as compared to the main browser
> screen.  A steady part of the console type of feel to it.  We need to
> check the narration signals on each interview so that the current
> selected state of speaker on or off persists and each page responds if
> speaker is active.  We also need to confirm that the logo icon that is
> the pulse of the narrator voice and also the mute is functional and
> that we only play the mitigating speech fillers if the speaker is
> turned on, on that page.  So, if the speaker is on that is A, if it is
> off that is B.  If A, play the normal, If B, if they hit the button it
> plays the filler and then changes their status to A so it will play the
> normal without the filler on the next screen it hits.  With the new
> favicon, I think we create a new animated version of the logo for the
> splash page and to replace the logo as the voice and mute on the app.
> For the voice on the app, I want make a voice visualizer type of visual
> that moves with the voice for the effect and for that to be connected
> or in a cluster in the top right.  On the Menu that opens for the jump
> to, we ned to give that home button a bit more pop and polish so it
> feels like an easy "save point".  I also want to categorize the
> questions in some way using a cool filter visual or just collapsing
> sections.  That many questions is too many to be helpful if you don't
> already know what you are looking for.  We need to apply the lower
> panel treatment to the file main page that has the brain model and the
> list so that the color and visual presentation is the same  in each and
> it looks and functions the same."

Mid-round addition (Adam, 2026-09-02, verbatim): "also, if the context
file has not been started yet, change that default banner 'Teach AI who'
and replace it with YOUR NEXT MOVE.  Change the lower section currently
titled 'YOUR NEXT MOVE' to 'YOUR WORKBRAIN'

In the newly created YOUR NEXT MOVE section, just preview of the next
prompt they need to complete or update.  This will be where updates to
confirm or update stale files and other stuff like that will go as well
when there are no build actions required.  This will feature the actual
Next Move oldest first, but can then expand to see them all or once they
finish the feature one, it should automatically take them to any other
open flagged value as a part of the maintenance.  Like when Word searches
for grammar and cover the whole doc starting from wherever you are.  Once
we start on a stale or incomplete record we go to the next open or stale
after."

## The plan, the flags, the calls

1. **Download expands.** The Home download row opens to a component list —
   each file with its presence state, per-file download, and "the full
   workbrain folder". FLAG: a real folder/zip download needs either a
   dependency (banned without cause) or sequential downloads. CALL: a
   minimal STORE-method zip writer in core (no compression, ~100 lines,
   tested against known bytes) — a .zip that every OS opens, no dependency.

2. **The universe becomes the app's canvas.** The splash's seven-lattice
   field replaces the wall panels behind every surface — one component,
   shared with the splash intro, identical faintness (consistency IS the
   ask). WallPanels benches like SplashStage did. FLAG: seven continuously
   turning lattices behind every screen is a standing GPU cost — measured
   before shipped; reduced motion gets the still field.

3. **Narration signal audit.** The stored pref persists today; the audit
   is that every surface RESPONDS to it. Verified per-surface, fixed where
   found wanting.

4. **The A/B filler rule.** A = speaker on: normal narration only. B =
   speaker off: pressing the mark plays the filler once, flips to A, and
   the NEXT screen reads normally with no filler. Requires finding what
   the "mitigating speech fillers" are in the current engine (the
   `drop` lead-ins) and wiring the state machine around them.

5. **The peaks take over the voice.** An animated W Peaks for the splash
   intro, and the narrator mark becomes the peaks as a VOICE VISUALIZER —
   three bars that move while reading (driven by the speech engine's word
   boundaries — Web Speech exposes no amplitude), muted = flattened, in a
   top-right cluster.

6. **Jump-to's Home button** gets save-point pop.

7. **Jump-to's questions collapse by section** — module groups, collapsed
   by default. (His "cool filter visual or just collapsing sections" —
   the sections ARE the filter; no second control invented.)

8b. **Home restructures around the queue** (the mid-round addition):
   fresh Home leads with YOUR NEXT MOVE (a preview of the next question
   to complete or refresh, oldest first, expandable), the old section
   renames to YOUR WORKBRAIN, and finishing the featured item sweeps to
   the next open/stale one the way a grammar check walks the whole doc
   from wherever you started. FLAG: the sweep is a sequencing feature in
   the runner, not a Home widget - it needs its own maintenance queue in
   core (open + stale detection) and lands as its own pass late in the
   round.

8. **The file main page wears the lower panel's treatment** — the brain
   model + list page matches the drawer's color and presentation.

## Pass log

### Pass 1 — the universe grounds the app (2026-09-02)

The seven-lattice field extracted to shared scenery: geometry in
core/splash/universe.ts (pure data), the component in
scenery/Universe.tsx + .css, and BOTH hosts render it - the splash
intro's globe (its wrapper keeps the fade-in arrival and the test seam)
and App's ground behind every surface. WallPanels retired to the bench
with its core and painters, the way SplashStage went;
tests/e2e/wall-panels.spec.ts is SUCCEEDED by universe.spec.ts, which
re-pins decoration's contract for the new scenery: aria-hidden and
unclickable, one component in two hosts, zero scheduled frames (CSS
transform only, where the wall needed a canvas clock), stilled wholesale
under reduced motion, faint under a cap. Exposure is per-host
(--universe-dim): the app lifts the field 1.7x because a dense screen's
furniture occludes what the intro's one card does not - probed with an
isolation shot (film/ground-iso) that also proved the paint stack above
the field is transparent all the way down, VB-111 exactly as written.


