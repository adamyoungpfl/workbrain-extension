# V2.8 — the morning after the show: Home sharpened, assist goes inline

## BUILT — 2026-08-27, five commits, final gate 1,878 unit / 555 e2e / 69 a11y
All eight items landed the same day the notes arrived. 2c7c4e4 (six-second count + Home
de-framed + banner gone + blue headers + YOUR NEXT MOVE), b9bed72 (the globe's flex-column
contract honoured — full square restored; the closed strip's face overlays the whole band,
centred in the visible black), 53de583 (the Skill Redeemer through the pack path with the
product's one sanctioned fetch and zero new permissions; Workbrain+ saying $42 a day with the
$5.25/hour framing, four ticked goods, one door to /work-brain/plus), d0e02a8 (the two-zone
capped flow — question zone yields and scrolls, answer stack always whole above the dock, on
every question kind; the assist sheet retired for the inline single step exactly as
described: Activated tag → one-line journey + icon-led copy + expander + AI door → copy
reopens the box as the paste spot, assistedAt semantics untouched). Laws that moved with the
work, recorded in place: the interior-scroll law replaced page-scroll for steps
(question-fill/save-note/button-cluster/deep-dive rewritten to it), VB-117's multiline
stretch retired, the qzone is a keyboard-focusable scroll region (axe's own catch), and the
V2.6 no-prices decision is reversed for display-only at Adam's instruction — checkout stays
on the site. AWAITING ADAM: the [DRAFT] pass (loading words already shipped in V2.7, plus
this round's tile/redeemer/Workbrain+ copy), the /work-brain/plus page, and the first real
pack file under model-citizen.org/packs/ (CORS-open) to light the redeemer end to end.

Sixteenth round, Adam's notes 2026-08-27, the morning after V2.7 shipped the splash show.
Two items arrived with reproductions already in hand (VB-135/136 below — screenshots in the
morning packet).

## The items

- **VB-131 · The splash's count: ten → six.** `SPLASH_BEATS.idleMs` 10000 → 6000, the doc's
  decision-4 note amended (Adam, today), sequence.test's pin moved with it. The loading words
  still cycle (~4 of the 8 show per pass).

- **VB-132 · Home cleanups.**
  (a) The all-current green banner ("Your file is current") goes — redundant beside the
  Context card's own Current status. The recs region keeps its element (focus target,
  aria-live) and simply renders nothing when the engine is quiet; real recommendations still
  appear exactly as today.
  (b) **The de-frame.** The white shell card and Home's own cool radial ground go: Home's
  sections sit directly on the app's standard light ground like every other surface, the
  chrome bar staying as a flat top row. One background level, not four. (This retires FLAG
  10's separate-world ground; the card grammar itself — white cards on the light ground —
  remains the differentiation.)
  (c) **Headers.** "YOUR FILES" and the tiles label set bolder and in the header colour, and
  the tiles label's text becomes **"YOUR NEXT MOVE"** [DRAFT].

- **VB-133 · The Skill Redeemer.** The TiM tile is replaced by **Skill Redeemer**: a tile
  that opens a sheet with one input — the code a person gets when they buy or commission a
  custom skill (they also receive the file by email). Redeeming lands the authorized skill in
  their file through THE EXISTING PACK PATH (VB-124's importSkillsPack: dedupe by id, caps,
  refusal voices — a pack is data, never code). Mechanics decision below. Degradation:
  offline or unknown code = the quiet voice, keep the code, nothing lost.

- **VB-134 · The Workbrain+ card.** The CTA becomes a real marketing piece for the brand —
  Adam: "rather than a vague menu with no prices (which people hate)". **This reverses V2.6
  decision 3 at Adam's explicit instruction — recorded here**; displaying a price and linking
  out is not commerce entering the extension (NORTH-STAR 4 stands: checkout stays on the
  site). The offer: **Workbrain+ — $42 a day** (the $5.25/hour employee framing; the 42 is
  deliberate), one year of access:
  one 90-minute session per month with a TIM · Workbrain Live (refreshes, alerts, usage
  data) · Workbrain Certified skills and resources · the monthly show-and-tell / customer
  CAB. All copy [DRAFT] for Adam's pass.

- **VB-135 · The Browse brain's top.** REPRODUCED: on the Context click-through the globe
  renders squashed into the upper right — off-centre, labels clipping the edge, a stray cue
  disc at the top left, dead space across the top. Investigate the stage's measurement
  (the projection centre looks computed from a mis-measured width) and fix; pin with a
  layout claim in file-slots/browse specs.

- **VB-136 · The collapsed drawer's strip.** REPRODUCED: fully collapsed, the strip's mark is
  cut by the bottom edge and the text/status hug the fold instead of sitting centred in the
  visible black; the grip floats half-covered above it. Fix: the collapsed strip centres its
  mark + label + count vertically in exactly the visible band, nothing covered top or bottom.

- **VB-137 · Actions anchored to the input.** On text questions, "Give me an example", "AI
  Assist" / "AI Assist (Recommended)", and the navigation buttons sit as a fixed cluster
  DIRECTLY under the input box — the actions live where the typing happens, instead of the
  helpers floating separately above the melt foot.

- **VB-138 · AI Assist, inline and single-step.** The sheet retires. Pressing AI Assist:
  the input collapses; an **"AI Assist Activated"** tag marks the spot; a one-line
  instruction bar pops below with a COPY control at its end (icon-led) and an expander to
  read the full prompt (collapsed by default, never assumed read). Copying clears the tag
  and reopens the input in the same spot with the new instruction: paste what your AI wrote
  here. A paste lands exactly as V2.5 decided (assistedAt stamp, recheck bypassed); a person
  who dislikes the answer is standing where they started — try again, or type their own.
  The nudge semantics (Recommended pulse on short unassisted drafts) are untouched.

## Decisions needed
1. **VB-133 mechanics** — how a code resolves, v1. RECOMMENDED: the code fetches a pack from
   the site — `model-citizen.org/packs/<CODE>.workbrain-pack.json`, static files you create
   per customer, CORS-opened on that path so the extension needs NO new permission (the
   manifest stays storage+sidePanel); the pack imports through VB-124's path. Alternatives:
   v1 accepts the emailed pack FILE (code UI later), or build the sheet UI now against the
   URL scheme and you stand the path up when ready.
2. **VB-132b confirm** — the de-frame reading above (shell + cool ground go; standard light
   ground; flat chrome row).
3. **VB-134 link** — where the card points: a new /work-brain/plus page you'll build, or
   /contact until it exists.
4. **VB-134 claim shape** — the honest basis under "$42 a day": billed as one year on the
   site? The card should say what the site charges.
5. **Header colour** (VB-132c) — the primary blue: yes / another.
6. **VB-138** — keep the "Open ChatGPT/Claude/…" service link inside the instruction bar
   (the old step 2's door): yes / copy-only.
7. **VB-137 scope** — the under-input cluster on text questions only, or nav under the
   answer area on every question type?
