# V2.9 slice 4 — the doors, the cable and the rocket

The last of the four splash-reveal slices. Slices 1–3 built the choreography
spine, drew it, and made the sections into devices; this one is the way out.

**This file exists because slices 1–4 were never written down.** The plan lived
in one session's context, and picking it up the next morning meant recovering
it from a transcript. Adam's words below are verbatim, so the next person to
open this does not have to.

## Adam's brief, verbatim (2026-09-01)

> For the Set Your AI Baseline button, let's make it like a 2 button cluster
> displaying like a single object. I want to make it feel loosely like the are
> choosing the narrated or silent baseline and that the narrated is the heavy
> lean, but optional. Like choosing which door you enter the rocket from.
>
> Below the Launch Button when dormant, should look like a simple control panel
> button in the modern theme and aesthetic of the site. It should have a
> "cable" that is attached to it that is glowing faintly. When the launch
> button is hit, the animation is a pulse coming through the wire, into the
> button and then the fade out and rocket sequence.
>
> On Launch button being clicked, I want to materialize a rocket that is color
> on theme with the panel and the app as the rest of the buttons and text and
> logos fade living only the dark background. The rocket should be most lightly
> tones, with pops of color from the app pallets. The animation is that as the
> rocket materializes and sort of visually rumbles and takes off upwards,
> behind it, a trail of white becomes the whiteout that then fades into the
> home page. That is a bomb cinematic open if we do it well and make it
> polished.

## The three sub-slices

Each lands behind a full `npm run check`, one commit each, per CLAUDE.md.

### 4a — the cluster · BUILT 2026-09-01

The baseline door becomes two doors that read as one object: narrated (the
heavy lean) and silent. Both go to the same place; which one is pressed sets
the narrator preference on the way in.

**The splash's own `NarratorToggle` goes with it.** A screen that asks "with
the voice or without" and *also* carries a mute toggle is asking twice — the
same objection BR-01 made when it deleted the third control from this screen.
The toggle is not lost: it lives in the interview header, which is where
somebody changes their mind.

**Where there is no speech engine there is no choice**, and the cluster
collapses to the single door it was. `NarratorToggle` already works this way
("not a disabled button, not a note explaining itself"); a cluster offering a
choice the device cannot honour would be worse than the toggle it replaced.

**What removing the toggle broke, and how.** `setPref` returns early when the
value it is handed matches the one in memory, and it was the toggle that
loaded the stored answer into memory. With it gone, memory held the default
`false` — so somebody who had the narrator on and pressed "Set it silently"
wrote nothing. The cluster loads preferences on mount and awaits that load
inside the press. Worth knowing before anything else on this screen starts
reading a preference: **the splash has no other reader.**

### 4b — the cable and the pulse · BUILT 2026-09-01

The launch door becomes a control-panel button with a cable hanging from it,
faintly lit. Pressing it sends a pulse up the cable into the button, and the
pulse is what starts 4c.

`core/splash/launch.ts` holds both clocks: `pulseAt` (where the light is, 420ms
end to end, accelerating because current arrives fastest where it lands) and
`idleGlowAt` (the wire's slow breath at rest, small enough not to compete with
the doors it sits under).

**Two things to know before touching this again.**

*The press is gated on a running loop, not on a copy of the loop's guards.*
`press` used to re-test `still` and the ref from outside the effect, which
missed the case where the effect bails because the cable reports no length —
a press in that state armed a pulse nothing was running and the door never
opened. `wired.current` is set by the loop that does the work.

*`.home` is visible from the first paint, underneath the splash.* Any test
timing the hand-off has to wait for `.splash` to go, not for `.home` to
arrive. A test that waited for Home measured Playwright's round trip and
asserted 420ms against it.

### 4c — the rocket · BUILT 2026-09-01

The rocket materialises, rumbles, and leaves upward; everything else fades to
the dark field; a white trail becomes the whiteout that hands over to Home.

`core/splash/rocket.ts` is the whole flight as one clock — `LAUNCH_MS` (the
1.6s of assumption #2) with every beat inside it. `SplashRocket.tsx` draws it;
`Splash.tsx` owns the seam: the doors call `launch`, not `leave`, and the
flight's end calls `leave` with the destination the press parked in a ref.

**Under reduced motion there is no rocket** and the hand-off is immediate,
which is what the splash already does. The law is unchanged: the still version
keeps the instruction, and the instruction here is "you are in".

**Four decisions the next person should not have to re-derive.**

*"Everything else fades" is one layer, not five.* The rocket's stage is the
dark field itself, fading in over the reveal — the doors, sections and lockup
sink together, and a stage that fails to paint leaves a working screen
underneath. The reveal is `inert` for the flight (`.splash-hold`): its doors
are still in the DOM under an opaque stage, and an invisible button that still
took an Enter could re-choose narration mid-air.

*The hand-off does not depend on the drawing.* `onDone` fires from the paint
loop when it runs and from a plain timer when it does not. A launch that
cannot draw itself costs the cinema, never the arrival.

*Escape mid-flight finishes the transition to the chosen door* — not to Home.
The person already answered; a shortcut that changed their answer would be the
screen overruling them. Before any press it still means Home, as ever.

*The whiteout is total at `LAUNCH_MS`, by test.* The splash's ordinary 320ms
exit fade starts from that frame — white, into Home — and a whiteout short of
1 would put a dark edge in the cut.

Filming it: `node scripts/film.mjs --launch` presses the launch key (pulse
included); `--baseline` presses the loud door, same rocket without the pulse.

## The polish pass, verbatim (Adam, 2026-09-01)

> I do want to smooth it. Let's do a dissolve from white to the target
> screen, I like the idea of smoke then sort of digitally dematerializaing
> like a fog disappearing to reveal either the home page or the baseline page
> baed on whichever button they click.
>
> Reduce the padding between "Workbrain" and the tagline below it. It needs
> to read like a lockup. Too much blank space right now.
>
> Let's make a squiggly line to connect the Start to Finish section to the
> CTA to Set Your Baseline. Make the line transition from the fuscia to the
> blue for the button.
>
> In the same pattern as the sections above, make the top section fade in as
> "SET YOUR PROMPTING BASELINE" with BASELINE in the blue with "I'll set my
> AI baseline in silence, like a professional" fades in underneath it as a
> secondary link. Give the whole thing a glowing outline so it gives the
> signal that it is a single element that is an action. Let' make that and
> the Launch offset at the bottom as the two acton buttons.
>
> Same with the Launch Workbrain button. I want to use the color pallete to
> make the whole thing feel on theme. But have an outline. A line from the
> above "Set Your Baseline" cluster. Blue that transitions to the aqua green
> from 15 above. The Launch button should have a glowing outline, transparent
> background in the aqua green. The squiggly below should flow towards the
> bottom right corner but go all the way to the corner. If it can't go to the
> corner edge because of a hidden border, we can fade to black so it looks
> purposeful.

### Built 2026-09-01, and what the next person should know

**The fog fixes the seam by ORDERING, not by covering it.** The destination
is opened at the whiteout (`onWhiteout` → App's `onBaseline`, which no longer
ends the splash; the splash always ends itself via `onDone`), the fog clears
onto it, and the splash unmounts last. `tests/e2e/splash.spec.ts` counts
naked-Home frames in-page and requires zero.

**The fog's clock anchors to the first PAINTED frame after the whiteout, and
holds solid white first (`DISSOLVE_HOLD_MS`).** Two real failure modes, both
hit while building it: the interview's mount can block the main thread
between two ticks (a wall-anchored fog loses exactly those frames), and the
interview renders `null` until `wb:answers` loads (fog that starts during
the read clears onto Home). White held is indistinguishable from white
animating — the hold is where both hide.

**The doors became parts.** `RevealPart` gained `baseline` and `launch`
(`doors` is gone); they arrive on their own beats and settle leaning -12/+16,
carrying the sections' alternation down the screen. The two new links are
gradient squiggles in the reveal's own `defs`; the cable is rebuilt at mount
from a measurement (key to panel corner, settle lean compensated from core's
own table) with a gradient tail-fade — Adam's "fade to black" alternative,
applied always.

**The cluster is typography in a glow.** Heading strings are split
lead/span like the sections' (`splashBaselineLead`/`Span`), and the whole
heading stays in `splashBaseline` because it is the door's accessible name —
an e2e pins `lead + " " + span === whole`.

## The hold pass, verbatim (Adam, 2026-09-01, second polish message)

> Reduce the padding between the bottom of the workbrain logo lockup and the
> tagline below it. There should be about half the difference in space
> between "Workbrain" and "How you do..."
>
> Make the tagline text pop a bit with some color iconography that focuses
> attention on the the words "you do" and and "AI does" different from the
> other letters and not visually connected but thematicatelly connected. I
> also want to animate the underline everything.
>
> Also, cut the "Nothing leaves your" animation to start as "Anything" and
> then crosses out and becomes "Everthing". We just need that tighter on
> timing.
>
> I want to make the Basline prompt button and Launch button a little
> cleaner and less linear. Make the Set your Prompting Baseline a circular
> button. The button is one where when you hold it it loads for a couple of
> seconds. Make the circle outline grow in thickness and color brightness.
> The button action counts down 3, 2, 1 and the background around everything
> but the countdown numer fades to white, so the number needs to be in color
> that is bold enough to be against black or white and pop.
>
> Add a simple toggle layer that lets you make the button sound on/off
> before you press down to activate the button.
>
> Same pattern for the Launch Workbrain button. Circular design, looking
> like a control panel piece approapriate for the aesthetic. Hold down
> starts a 3, 2, 1 countdown making the button and over the 3 second the
> entire screen to white to transition to the home page.
>
> In this way, both action button, have dissolve to the same complete white
> surface that can then dissolve to the target location without exposing the
> other path.
>
> on the opening sequence, I want to make the tiles smaller and more
> irregular in shape. I also want to make them all black and white, like a
> bold pencil sketch. that fades to white in same flurry as the current one,
> just this relies onlh on the changing of images and not on the changing of
> color to suggest motion or build. I want to take a a faint light version
> of this background and make it the underlying canvas background for the
> app in the home and interview canvas and baseline question and anywhere we
> have white or light background surface so there is a continuity to the app
> for dark background look like A and light backgrounds look like B…

### Built 2026-09-01/02, and the judgement calls a next person needs

**The elimination reads "Anything → Nothing", not "→ Everything".** The
message's literal words would end the device on "Everything leaves your
browser" — the inverse of the promise the product rests on — so it is read
as a slip. Two beats now, his "tighter on timing"; one string array and
`CLAIM_WORDS` to change if the intent was different.

**The countdown and the flight are ONE length (3s), and the rocket lives.**
The launch brief says the countdown takes the screen to white and never
mentions the rocket — but a 3-2-1 count is what happens BEFORE a launch, so
the flight was retimed to the count (`LAUNCH_MS = COUNTDOWN_MS = 3000`,
asserted equal by test) and the digits ride over it, mission-control style.
The baseline key has no flight: the count's own `white` ride does it. Both
land on the identical full-white frame at zero; the fog needs no knowledge
of which key was held. If Adam wants the rocket gone entirely, it is the
`flight` prop's two call sites.

**Holding is the new press, and a plain click arms under reduced motion.**
The charge is choreography; the CHOICE is the load-bearing part. Keyboard
holds work (Space/Enter down…up, same clock). A tap under full motion
deliberately does nothing — the hold is the confirmation this product
refuses dialogs for.

**The narrated-or-silent choice went back to a toggle**, because one
circular key cannot be two doors — Adam asked for the toggle by name
("button sound on/off"). It is the interview header's own `NarratorToggle`
(one preference, loads-then-writes, hides without a speech engine), first in
the tab order per the 2026-09-02 precedent. The digits are spoken through
it. `splashBaselineSilent` is retired in strings.ts, recorded not deleted.

**The sketch is drawn in ink, not filtered.** Desaturating the pastel set
made fog — the grounds share one light luminance. `sketchTextures()` hands
the painters a gray ramp in place of the wheel, so dark panes carry white
strokes and light panes dark ones. The splash tint cycle is gone (his
"changing of images, not color"); the opening stillness (panels pinned to
their first picture) survives. The app wall (`WallPanels`) draws the same
textures faint under its VB-97 axe cap — the cap is why the light version
whispers; raising `WALL_OPACITY_MAX` is the one constant if Adam wants it
louder, and it is a decision against a recorded readability rule.

## The ring pass, verbatim (Adam, 2026-09-02)

> Ok. Move the "Set Your Prompting Baseline" button down a bit and have the
> squiggly line connect more aorund the "3 O'Clock mark" on the circle and
> drags out to the right a little so the color transition has space to
> happen naturally without the line looking too small and crammed in.
>
> Have the squiggly from the set your prompt button to the launch button
> connect from the 5:30 mark on the circle and then connect around the 7-8
> O'Clock mark on the lower button with the line squiggling out a bit to
> left or maybe down below the connection point vertically and back up to
> again give enough space for the color transition to be visible and not
> feel crammed in.
>
> For the buttons, make each of them a thick outline of color and in the
> outline, we have a talking person silhouette icon or symbol on the right
> and an icon that is the same with a strike through it to indicate without
> guidance on the left. They should each be a different color and which ever
> side the click the rest of the outline fills in that color during the
> countdown and launch sequence. The thicker outlines also allows the
> connections for the squigglies to be hidden so you don't see any overlapp
> or seams.

### Built 2026-09-02

The squiggles route by his clock bearings — `rim(key, hour)` in the reveal's
drawLink, tips tucked 8px inside the 10px band so no seam shows. The sound
toggle folded INTO both rings as the two silhouette sides (struck at nine,
talking at three); the held side writes the narrator preference at the arm —
4a's press-is-the-answer law, back in a circle — and the armed ring fills in
the key's colour across the count (synchronously full under reduced motion;
the fill loop would break the zero-frames law). Where no speech engine
exists the sides collapse to one quiet hold-target, as ever. Two traps worth
remembering: the launch part must HUG its key (full-width, pulled up across
the baseline's row, it intercepted every pointer aimed at the baseline's
right chip — half the suite red with nothing visually wrong), and a 44px
target under a sub-pixel transform can measure 43.99998, so the sides are
46.

## The split pass (Adam, 2026-09-02, from a reference image)

A reference screenshot — "Begin Journey (Narrated)" filled, with a compact
struck-speaker "Silent" segment beside it — with the note that "the split of
the button with the labels distinct is a good pattern", and that the
reference's arrow and progress bar can be left out.

### Built 2026-09-02

The circles became split pills: the narrated main filled on the key's DEEP
token (white text ~15:1 — the reference's bright fill would sink the 4.5:1
floor with our hues), Silent beside it with the struck silhouette, labels
carrying their own "(narrated)" qualifier in sentence case. Everything
load-bearing survived the shape change: holding is still the press (the
charge is the pill's inset ring thickening and glow growing, via a
`--charge` custom property), the held segment still writes the narrator
preference at the arm, reduced motion still arms on click, no speech engine
still collapses to one quiet pill. The `splash-holdkey`/`-side` classnames
and `data-side`/`data-live` attributes carried over unchanged — they are
the seam the e2e suite and film script hold. The clock-bearing squiggle
anchors re-mapped from ring rims to pill edges (3:00 = right edge middle,
5:30 and 7:30 = bottom edge stations); tips end 6px inside the pill and its
opaque fill paints over them. The circle era's countdown ring-fill retired
with the ring — the count's drama lives on the stage's digits.

## The mock pass (Adam, 2026-09-02, third reference)

A drawn mock: label + "Narrated" caption in the left segment, "Silent" with
an icon in the right, one pill filled aqua with dark text and one outlined;
"make the silent button part offset in color to reverse mirror the main…
limited color backgrounds on default and use of color as pop"; the cluster
centred, baseline then "or" then launch, the squiggle from START TO FINISH
wiggling left-right down the centre into the array.

### Built 2026-09-02

To the mock: baseline outlined with a whisper-tint Silent, launch filled in
`--globe-node-2-solid` with INK text (~11:1 — white there would sink the
floor) and its Silent one shade deeper (`-mid`), captions under labels,
"or" between, everything on the centre line (the action leans went to 0 in
the reveal table; the sections keep theirs). The visible labels dropped
their "(narrated)" qualifier — it lives in the caption AND in each main's
accessible name, so the button still says its temperament to a screen
reader, and the name still begins with the words on the button. The
clock-bearing anchors retired with the side lanes; both action links ride
the centre with the plain wiggle geometry. Collapsed (no speech) pills wear
the plain label and name — no "Narrated" caption where narration is not a
thing the device can do.

## The type-and-load pass (Adam, 2026-09-02, two messages)

First: labels and tagline join the section type (caps, 17px/700/.06em; the
tagline's second row keeps its case and colours), rounded rectangles,
purple baseline outline with a light purple tint ground and a deep-purple
Silent, launch keeping its aqua with an outline of its own and a
TRANSPARENT Silent whose marks are the aqua ("the mirror inverted"), longer
untangled centre squiggle, "or" at header size, both keys the same box,
and the "SET YOUR / PROMPTING BASELINE" break authored (derived from the
one string).

Second, the interaction: **click-to-load replaces hold-to-arm.** "If the
user clicks on Set or Launch buttons, let's do a color load that does a
color fill of the entire button from left to right. Once it loads the whole
screen dissolves to black to initiate the rocket launch sequence." The
click commits; the fill (a translucent wash of the key's colour, scaled by
the same `--charge` the component always wrote) is the ceremony; arming is
the fill completing. Keyboard comes free — Enter and Space are clicks on a
button by birthright — and the release/discharge choreography retired with
the hold. **Both routes fly the rocket again** (the fade-only mode remains
in the stage, unreachable); the flight's cover lengthened to 480ms so the
"dissolve to black" is seen happening. The cable's corner-run guard eased
to 24px: on a short panel a short hop drawn beats a fallback pretending
nothing changed.

## The radio-and-simplification pass (Adam, 2026-09-02)

Four asks. **The radio:** NASA-flavoured transmissions spoken through the
narrator's own engine while a voiced hold loads — "Tower is clear for
systems check." on the baseline, "All systems go. Commence launch
sequence." on the launch — cut off by a backoff (`stopSpeaking`), handed to
the counted digits at the arm. Words, not audio assets: nothing for the
guardrails to meet, and Silent holds load silently because silence was the
choice. **The hold returned** the same day the click shipped: "require the
hold down until they are full to activate so that someone can backoff" —
the left-to-right fill stays, but it only advances under a held pointer
and drains on release. **Longer bows** above and below the "or". **And the
simplification:** after the whole pitch lands (REVEAL_REST ≈ 8.25s since
the elimination cut), it folds away — the time section and its route, then
the privacy section and its routes, sections' spaces closed by the keys
rising (80px, then 184px total), the action bows last — until
`REVEAL_SIMPLE` (11.4s): lockup, tagline, two keys. The STILL frame keeps
painting `REVEAL_REST`, the full composition, because the still version
keeps the whole instruction; the no-motion-after law moved to
REVEAL_SIMPLE; the cable measures its corner run for the FINAL rest, where
the person lingers. One test de-raced: the pulse sampler holds the Silent
side, because the radio's first engine touch can stall the exact 420ms it
samples.

## The open-and-eta pass (Adam, 2026-09-02, last of the day)

**The time chips.** A small tab under each key: "AVERAGE COMPLETION TIME"
with a to-the-second figure drawn at random per open in Adam's ranges
(baseline 2m04s–3m12s, launch 12m38s–23m55s). **Flagged, deliberately:** a
random number wearing the word "average" is a claim the product cannot
back, on a screen whose other claims are all checkable — and this product's
whole posture is that its claims check out. The honest evolutions if wanted
later: label it "Typically 2–3 minutes" (a range is a promise the copy can
keep), or measure real completions locally-with-consent and mean them. One
string and one function either way.

**The majestic open.** The mosaic wall retired (mosaic.ts + SplashStage
stay on the bench, tested) for the brain model's own idiom: a huge
pencil-grey mark turning at 15% on the panel's light, the title card
building over it — lockup, tagline, "by Model Citizen" — then the swell,
now wearing the FIELD'S DARK, carries everything to black and the sequence
proper begins. Same clock, same beats, same reduced-motion law (the intro
never mounts; zero frames). The faintness lives on the wrapper's keyframe —
the first cut pinned 15% to a class the component doesn't carry and got a
graphite wall instead of a backdrop, which is the kind of miss only a film
catches.

## Pass 15 — the universe, the admiration hold, and one purple family

The brief (Adam, 2026-09-02 — recovered from the session summary after a
context compaction ate the original message; the ellipses are the
summary's, not edits): "Make the 'You do' in the tagline the same purple
as we are using below. On the splash, let's stretch that sequence out
after the Model Citizen for another 1-2 counts. Then slowly fade it all
the way to black so the transition is smooth between them. For the giant
logo. I want to do a design of the background that is several of those at
varying perspectives and distances like you are in a white universe that
has maybe infinite of these brain model shapes that are all sort of
translucent that are spinning steadily and constantly at their own paces…
cinematic like a video game producer… hold it for just a second to admire
the elegance and then it fades elegantly to black without the messy blips
of other screens."

**Built (commit ee1076a; this record landed one commit late — the doc law
slipped in the same compaction and is restored here):** seven lattices in
a fixed field — near is size and presence, far is smallness, faintness
and a breath of blur, each with its own slow container turn (70–160s,
some reversed) over the shared orbit so no two read alike. The card
completes at "by Model Citizen" ~3.3s and HOLDS to 4.9s; the swell then
takes a full second to carry the whole scene to black together. That
retimes SPLASH_BEATS (reveal 5.9s) and supersedes the pinned 4–5s reveal
window — the sequence.test pin moved with the decision. "You do" joined
the purple family (--splash-link-end).

## Pass 16 — the audio cues, and what records them later

The brief (Adam, 2026-09-02): "Love it.  Ok.  Let's work out the audio
queues for the sequence and then we are done with this part and ready to
get back into the app." And mid-build: "For the sound effects or audio
clips we need, we can use the Elevenlabs API which is what we will use to
record all of the canvas narration once we are final approved copy on all
questions for context and then skill interviews."

**The cue map, in sequence order.** Every fixed line lives in
`src/panel/voice/cues.ts` — one named registry, one call site per moment,
narrator-read today and clip-swapped there alone when the ElevenLabs
recordings exist:

| Moment | Cue | Consent |
|---|---|---|
| Title card builds (~1.4s) | "Workbrain. How you do anything is how your AI does everything." | Stored narrator pref ON — returners only; a first open stays silent because the sound choice is offered by the pills, not presumed by the poster |
| Voiced hold begins | "Tower is clear. Systems check." / "All systems go." | The person is holding the voiced side — the hold is the consent |
| Hold released early (charge > 12%) | "Standing by." | Same hold; an ARMED release stays quiet — the count owns the audio |
| The count | "3", "2", "1" | Narrator pref |
| After the "1", before the whiteout | "Liftoff." | Narrator pref |
| The fog lifts | — nothing. The destination's own narrator reads its first question; the splash's silence here IS the hand-off | — |

**Why the lines got shorter.** The radio originals ("Tower is clear for
systems check." / "All systems go. Commence launch sequence.") overran
the 1.6s hold and were cut mid-word by the counted "3" — a glitch, not
radio. Trimmed to fit; the originals sit in strings.ts's comment and
HOLD_MS is the one number to raise if they are wanted whole. Same story
one beat later: "We have liftoff." did not fit the 0.6s breath between
the spoken "1" and the whiteout, and "Liftoff." does.

**ElevenLabs is a recording tool here, not a runtime dependency.** The
guardrails rule a live call out three ways at once — no feature that only
works online, never transmit anything, no server of ours to hold a key.
So the plan the registry encodes: once interview copy is final-approved,
generate each cue (plus the three digits, plus any sound effects) ONCE
and ship them as bundled clips; `cues.ts` is both the wiring point and
the list of what to record.

**And one engine fix the pass surfaced:** `stopSpeaking()` on an engine
that never spoke now costs nothing. The first touch of `speechSynthesis`
— even a bare `cancel()` — stalls the main thread ~170ms while the voice
list loads, and the splash's skip button was billing that stall to its
own door (the reduced-motion press-cost test caught it at 591ms against a
420ms ceiling).

## Pass 17 — the intro tagline inked, the foot promise, and the baseline errand redressed

The brief (Adam, 2026-09-02, verbatim): "Make the tagline on the splash
page 2 lines, the way it is on the loading page.  Same color treatment but
the white letters are black on top and the gray are a darker gray than
they are so they read against the white background.

Add the light gray 'Saved on this device Nothing leaves' disclaimer down
on the bottom of the loading page.

Baseline Question

Narrator:  Ok!  To set your baseline, you just need to write one prompt.
Think of an action that you would normally do.  Start with a verb like
plan or edit or summarize maybe?

Prompt: One prompt…in your own words.  (Double space) 'You will run this
EXACT prompt in your own AI service next…'

Behavior: Next is disabled until text is detected in the input.  Center
the Next button and relabel it 'Submit' and move the skip button down
below as it's own link that is 'Not right now' and it links back to the
home page.

On second screen after next to paste the prompt, I want to create a 3
panel visual representation of the act of pasting into an LLM, copying
the final response button and pasting it back into this page.  I am
thinking like an airlines safety manual kind of vibe to it that stays in
the workbrain aesthetic.  That should replace the little description text
block below.  Make the first block in the story board a link to copy the
prompt again in case they need to as a part of the instructions.  The
second box should open into a drop down or some kind of way to pick a
common service and when they click it, it opens that URL in a new window
and saves their preference to the file.  The final button in the array
for pasting it back should just activate a glowing outline of the text
input box below so it is clear where they are pasting if they need to
check.  Move the Establish my baseline and Not Now box cluster to be
fixed below the input box with the proper padding."

**The intro tagline.** richTagline moved to Splash.tsx (shared, no cycle)
and the intro card now renders the loading page's own two-line grammar,
re-inked for white: row one --ink, row two --ink-2, and the two pops
stepping down to new white-canvas family members (--splash-you-ink
#8B45F5 at 4.92:1, --splash-ai-ink #0E7A8C at 5.02:1 - the dark-stage
originals measure 3.24:1 and 1.75:1 on white, under the floor). One trap
worth its sentence: the reveal's own `.splash-tagline-line:first-child {
--ink-inv }` outranked the intro's colour by file order and row one
painted white-on-white - the probe caught a tagline with only its purple
mark visible.

**The foot promise.** `.splashreveal-disclaimer` - savedNote and
privacyNote in light gray (rgba 237,239,243 at .58, ~5.6:1 on the field),
fixed to the panel's bottom edge, aria-hidden because every flow screen
already carries the same two facts for the accessibility tree.

**The baseline question - reworked IN PLACE, and the doc owes the story.**
The first build made a fresh BaselineQuestion surface and intercepted the
door path in Flow. The suite then listed what that surface had silently
binned: the seed typewriter, the idea stack, the advice-into-declaration
note, the wish-into-order hedge repair - all of it Adam's own earlier
briefs, all living in the REAL prompt screen (StepView's `bare` +
`promptOnly`). So the detour was reverted and the brief landed where the
screen lives: the new question and hint went into the `goal_want`
override (core, where question wording belongs - narrator and heading
stay one sentence set), and the nav got a door-scoped dress
(`baselineDoor` prop -> `doorPrompt`): Submit, centred, disabled until
text; "Not right now" under it, going Home. A plain-path session meeting
the same question keeps Next/Skip, because there "not right now" would
abandon an interview mid-gate. panelQ's 120-char cap exempts goal_want
with its reason in the test: Adam's verbatim panel copy on the one screen
that owns the whole panel.

**The storyboard.** Three numbered stroke-drawn cards in flight order,
each the control for its own step: (1) re-copy the prompt, (2) open the
pick-your-AI row - plain anchors from the one service list (VB-105),
opened by the person's own click, the pick saved into `goal_service`
through the same applyAnswer the gate uses - and (3) light the paste box
(outline pulse; reduced motion keeps the ring, drops the pulse) and focus
it. The old description block retired with its essence relocated: the
recopy line is panel one's sub-line, and D1's fresh-chat memory warning
is panel two's caption, said at the step where it acts. The doors pinned
to the panel's bottom with the save note inside the bar.

Spec repairs, honestly listed: baseline-door and baseline-prompt press
Submit where they pressed Next, and the field's accessible name follows
the reworked question. Nothing else in either spec changed.

## Pass 18 — the rename, the grown pair, the synced count, and the pending row

The brief (Adam, 2026-09-02, verbatim): "Change the name of the 'Set Your
Prompting Baseline' button to 'INITIATE PRE-LAUNCH'

Get rid of the little squigglies in the loading sequence above and around
the buttons.

Make the 'saved on device' text down at the bottom a bit more faint.

for the the final action on the animation, I wannt to use grow a short
squiggly between each buton and 'OR' with the appropriate padding.  I
want the lines and text together to transition from teh purple above to
the green below so that the center point of the transition is at the
vertical center of the word 'OR'.   Make 'OR' capital so that is more
surface space to visually detect the color in the text.

Then we just need to make sure that the countdown narration (3, 2,
1...Liftoff) is synced to the visual 3, 2, 1 and the rocket taking off.
So it makes sense to the user.  Same on the radio bits.  They need to
start on the initial click and stop if the click is raised on the button.
But the audio clip syncs to the loading sequence so it has to start at
the beginning of the click.

I also want to add 'PRE-LAUNCH BASELINE' as Next Moves tile if it has not
been completed so that we have a way to go back and grab it before the
final proof and make the user aware that it is still pending before their
workbrain experience is complete.

I also want to fix the favicon image to make it something more bespoke
and polished now that the app has a more polished feel.  Let's explore a
few concepts that will render well on the chrome pallette and will look
good at the top of the app so that it doesn't look odd with the app name
being listed right next to each other twice in the users visual space.
With using the negative space, I think we can get a version that looks
good on the panel as the Claude icon that is colorful and erratic but
distinct and perceivable as a button.  I just need to make sure that it
is unique and vibrant enough to be easy to spot and activate when looking
at the browser as a normal day to day user without the app open and doing
your normal work."

**The rename.** splashBaselineLabel is 'Initiate pre-launch' (the pill's
CSS carries the caps); the two-line break re-derives as first-word /
rest, so INITIATE sits over PRE-LAUNCH. The accessible name follows.

**The squiggles.** SCOPED CALL, flagged for Adam's veto: "above and
around the buttons" is read as the button region - the privacy-into-
baseline route, the baseline-to-launch route and the or-bow are gone; the
two section links at the top of the screen (tagline-to-minutes, minutes-
to-file) remain. If "above" meant ALL of them, it is two lines to remove.

**The grown pair** (`orPairAt`, core): at 5.45s - just after the parts
settle, unmistakably the last thing that happens - a short squiggle grows
from OR outward to each pill, 6-8px padding at every landfall. Both ride
ONE userSpaceOnUse gradient re-anchored every frame symmetric around OR's
own centre, so the purple-to-green hand-off crosses exactly at the word.
OR itself is capital (the string, not a transform) and wears its slice of
the same transition via clipped-text gradient; both mixes sit near the
endpoints' own 6:1 contrast on the field.

**The foot,** fainter: 0.58 to 0.45 alpha - ~4.6:1, the floor's edge, one
step darker would cross it.

**The sync.** The real bug: digits gated on the STORED narrator pref,
which the arm writes through async storage - the count's "3" could beat
the round trip and go unspoken. The side someone holds IS the sound
decision, so it now rides the callbacks (HoldKey onArmed(voiced) through
BaselineKey/LaunchDoor - held across the cable pulse's flight - into
Splash's chosenVoiced ref), and the digits and liftoff gate on it
directly; the stored pref merely follows into the interview. The radio
already starts at the press and stops at the raise; its baseline line
trimmed again ('Tower is clear.') because "Systems check." was still
under the arm at 1.6s and the count stepped on it.

**The pending row.** Home's list opens with PRE-LAUNCH BASELINE (rocket
glyph, 'Still open. Take your starting point before the final proof.')
while no baseline run exists - hasBaseline(report), core, tested. The
row's door takes the splash door's exact route (same flag, same landing
on the goal question). The proof's own baseline run closes it naturally.
An open door, not a nag - no guilt framing, per guardrails.

Spec repairs, honestly listed: home.spec's row census counts seven with
the pending row leading; its height budget moved a third time (1.65 to
1.75 panel heights) with the cause written in the test - a ~56px row that
REMOVES ITSELF once any baseline exists. baseline/splash suites follow
the renamed strings automatically.

**The icon.** Exploration, not a ship: four concepts drawn from the
lattice's own geometry (Ember Node tile / Cut Lattice negative-space
punch / Open Constellation no-tile / W Circuit monogram), published as an
interactive artifact - a real light+dark toolbar simulator at true 16px
among gray neighbor icons, size ladders on split grounds, and side-panel
header mocks probing the name-twice concern. The manifest keeps its
current icon until Adam picks.

ROUND TWO (Adam, same day, verbatim): "On the logos, Open Constellation
is the leader.  I would like to see W Circuit in negative space and not a
black background in a couple of ways." Done on the same artifact, same
URL: Open Constellation wears the leader badge and seats by default; the
black-tile W Circuit is retired for W CUTOUT (the letter punched clean
through the gradient tile - the toolbar shows through it) and W PEAKS
(no tile at all: three erratic peaks in the mark's colors, the W living
in the two valleys between them, FedEx-arrow style).

DECIDED (Adam, same day: "W Peaks is the winner.  Well done on that.") -
and shipped: the artwork lives once as design/icon-peaks.svg (the
artifact's exact winning geometry), scripts/icons.mjs renders it to
public/icons/{16,32,48,128}.png through Playwright's own Chromium with a
transparent ground (no new dependency for a four-times-a-year image job),
and the manifest's icon table is unchanged - it already pointed at those
four paths. The board artifact stays up as the record, marked with the
winner. Open question left deliberately unresolved: whether the app's own
header one day drops its text and lets the mark stand alone - that is a
product call, not an icon call.

## Pass 19 — the fork, seated

The brief (Adam, 2026-09-02, verbatim): "Ok.  We need to add some padding
between the ends of the squiggle lines and the buttons.  Make sure the OR
is perfectly centered between the buttons and that there is enough
squiggly visible to see the color changing, the launch workbrain button
can move down a little to make space.

Make the Launch Workbrain label 2 rows, so it mirrors the look of the
Initiate Pre-Launch ont that.

Slightly offset the Intiate Pre-Launch button to a little right of center
and the Launch Workbrain button a little left of center.  It will create
some space for the squiggly and also add a little style to it."

**Built, and PROBED rather than eyeballed:** landfall padding 8-to-14 at
the pills (8 at OR); the launch label splits first-word/rest exactly as
the pre-launch pill does; the stagger is translateX(plus or minus 14px) -
whole pixels, transforms not layout, so the 44px targets never move and
the squiggles (which measure the keys every frame) follow by themselves.
Centering took three probe rounds because OR and the launch pill travel
together in flow: the negative top margin on OR (-11px, into the eta
chip's slack) and the launch part's margin-top (23px) are the two levers,
and the probe script read 49px above OR's centre and 49px below on the
720 viewport before it was deleted. The first drop (18px) rammed the
launch eta chip into the fixed foot disclaimer - caught on the probe
still, reclaimed by pulling OR up instead of pushing launch down.

## Open, and worth Adam's word

1. ~~**Do both doors launch, or only the launch door?**~~ **BUILT to the
   default 2026-09-01, still Adam's to overrule:** both launch; only the
   launch door has the cable. "Which door you enter the rocket from" reads as
   *both*, which is also the better product — leaving the splash is one moment
   however it is left. The two doors arrive at the same rocket by different
   routes: the launch door through its pulse, the baseline doors directly.
2. ~~**How long may the launch take?**~~ **BUILT to the default 2026-09-01,
   still Adam's to overrule:** 1.6s, and it is one number —
   `LAUNCH_MS` in `core/splash/rocket.ts`, with every beat of the flight
   derived inside it. The rumble, the climb and the whiteout sit between a
   press and the thing the person pressed for, and cannot be skipped because
   they *are* the transition (Escape only finishes them early).
