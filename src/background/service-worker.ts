/**
 * Release 1 has no content scripts and no host permissions, so there is nothing
 * to route yet. This file exists only because the manifest declares it.
 * See docs/ARCHITECTURE.md (message contract) for what lands here starting Release 4.
 */
export {};
