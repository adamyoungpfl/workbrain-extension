import './tokens.css';

/**
 * Placeholder shell for R1-01. Replaced by the real surface router
 * ('home' | 'flow' | 'sheet') at R1-12 — see docs/ARCHITECTURE.md.
 */
export default function App() {
  return <div>Workbrain</div>;
}
