{
  "$comment": "Single source of truth. `npm run tokens` generates src/panel/tokens.css from this. Never hand-edit the CSS. Contrast ratios are measured against --canvas unless noted.",
  "color": {
    "canvas":   { "value": "#FFFFFF" },
    "surface":  { "value": "#F6F7F9" },
    "sunken":   { "value": "#EDEFF3" },
    "ink":      { "value": "#15181D", "contrast": 17.79, "level": "AAA" },
    "ink-2":    { "value": "#525A67", "contrast": 6.96,  "level": "AA" },
    "ink-3":    { "value": "#6B7484", "contrast": 4.71,  "level": "AA" },
    "ink-inv":  { "value": "#FFFFFF" },
    "divider":  { "value": "#E2E5EA", "role": "decorative separators only — NOT for control boundaries" },
    "border":   { "value": "#D4D8E0", "role": "resting edge on non-interactive cards" },
    "border-i": { "value": "#8A92A0", "contrast": 3.13, "role": "every interactive boundary. WCAG 1.4.11 requires 3:1." },
    "dock-edge": { "value": "#7C8494", "contrast": 3.76, "role": "V1.4 VB-22. The interactive boundary INSIDE the file drawer's chrome, where the ground is the drawer's own stage rather than the canvas. --border-i's 3.13:1 is measured against --canvas and falls to 2.92:1 on --surface, which is the stage List's bar and nav take — under the 3:1 floor, so the dock steps once further down the same neutral ramp. Nowhere else: on the canvas --border-i is still the boundary." },
    "primary":       { "value": "#2A4FCB", "contrast": 6.82, "means": "Workbrain is asking you to act" },
    "primary-hover": { "value": "#1F3FA8" },
    "primary-tint":  { "value": "#EAEEFB" },
    "violet":        { "value": "#6A3FD1", "contrast": 6.47, "means": "this belongs to your AI — read only" },
    "violet-tint":   { "value": "#F2EDFD" },
    "violet-hover":  { "value": "#E8E0FC" },
    "amber":         { "value": "#8A5300", "contrast": 6.33, "means": "time is asking you to act — drift, staleness" },
    "amber-tint":    { "value": "#FFF4E2" },
    "amber-line":    { "value": "#E8A33D" },
    "green":         { "value": "#0B6E4F", "contrast": 6.25, "means": "done, current, healthy" },
    "green-strong":  { "value": "#064A34", "contrast": 10.45, "level": "AAA", "means": "V1.1 VB-07. The same 'done, current, healthy' family at full strength — the file tree's ACTIVE section against --green's already-reached ones. Reached vs current has to read as one colour at two strengths, not 'coloured vs grey' (the ported design). On a light canvas 'stronger' is deeper, not lighter: the sibling app mixes its accent toward white for the weaker state because it sits on a dark surface, and doing that here would push the weaker state under 4.5:1." },
    "green-tint":    { "value": "#E7F5EF" },
    "danger":        { "value": "#B4231E", "contrast": 6.56, "means": "something failed and you can fix it. Field errors only." },
    "danger-tint":   { "value": "#FDECEA" },
    "overlay":       { "value": "rgba(21,24,29,.42)", "role": "Sheet backdrop only — the one overlay in the system" },
    "toast-check":   { "value": "#6EE7A8", "role": "checkmark color inside a Toast, which sits on --ink" },

    "brand": {
      "$comment": "V1.1 VB-01. The Model Citizen node-graph mark, redrawn from ../modelcitizen/public/mark.svg. This is the one multi-colour thing in the system and the only reason a nested colour group exists — every other colour above carries a single meaning the interface uses to say something. These say nothing: the mark is decorative (aria-hidden) and always sits next to the real 'Workbrain' wordmark, so no contrast ratio is claimed and none is needed. Never use one of these for text, an interactive boundary, or a state. Five gradient pairs + one edge stroke, matching the source export exactly.",
      "node-1-from": { "value": "#2F5FE6" },
      "node-1-to":   { "value": "#53A9DC" },
      "node-2-from": { "value": "#14B8C4" },
      "node-2-to":   { "value": "#3CBCD0" },
      "node-3-from": { "value": "#6366F1" },
      "node-3-to":   { "value": "#6C6FF2" },
      "node-4-from": { "value": "#3D74E5" },
      "node-4-to":   { "value": "#48C4D2" },
      "node-5-from": { "value": "#4968BA" },
      "node-5-to":   { "value": "#5E87EB" },
      "edge":        { "value": "#5B7FD8", "role": "the graph's connecting lines — carried at a per-line stroke-opacity that fakes depth" }
    },

    "globe": {
      "$comment": "V1.2 VB-14. The Brain globe's own palette — the second and last nested colour group, for the same reason as `brand` above: these are the parts of one picture, not meanings the interface uses to say something. Deliberately NOT the `brand` values. Those were sampled from a flat single-pose export on a white page and are light enough to disappear against each other once a node is a shaded sphere; docs/V1.2-REFINEMENT.md's VB-14 iteration three picks a deeper palette and a dark stage on purpose, and calls that stage 'the one sanctioned dark surface in the product, contained inside the drawer'. Each sphere is a three-stop radial: `highlight` (shared, offset off-centre), then its own `mid`, then its own `deep` at the rim — a pair per sphere plus one highlight, which is what makes a node read as lit rather than as a flat disc. `label` and `focus` are the only two here that carry information, and both are measured against `field`. V1.4 VB-22 widened this group's job from 'the globe's palette' to 'the dark stage's palette': the drawer's whole chrome — its mode bar, its section count, its grip, and the docked nav's buttons above it — is one surface with the stage, so it dresses in `field`, `panel`, `label`, `detail-key` and `focus` rather than in a second dark palette that would have to agree with this one. V1.5 VB-24 turned the five `*-solid` values up — an answered orb had to read as lit rather than as coloured — and added a `*-muted` beside each one, because an unanswered node stopped being a hollow ring and became the same orb turned down: same hue, ~44% of the way to the stage's own slate, which measures a hair under half an answered orb's luminance at every depth while staying an object against the field. VB-25 added `unified*` for the one colour the whole model resolves to when the file is complete and current.",
      "node-1-mid":  { "value": "#3E77E8" },
      "node-1-deep": { "value": "#0C1C55" },
      "node-2-mid":  { "value": "#16A5BE" },
      "node-2-deep": { "value": "#052E3D" },
      "node-3-mid":  { "value": "#6062F0" },
      "node-3-deep": { "value": "#16155C" },
      "node-4-mid":  { "value": "#2F93DE" },
      "node-4-deep": { "value": "#08294A" },
      "node-5-mid":  { "value": "#7189EE" },
      "node-5-deep": { "value": "#1A2461" },
      "node-1-solid": { "value": "#5A93FF" },
      "node-2-solid": { "value": "#2ED6F0" },
      "node-3-solid": { "value": "#8A8CFF" },
      "node-4-solid": { "value": "#4FB6FF" },
      "node-5-solid": { "value": "#93A9FF" },
      "node-1-muted": { "value": "#4165AF" },
      "node-2-muted": { "value": "#288BA7" },
      "node-3-muted": { "value": "#5C61AF" },
      "node-4-muted": { "value": "#3B79AF" },
      "node-5-muted": { "value": "#6172AF" },
      "unified":      { "value": "#9CDCFF", "contrast": 13.26, "role": "V1.5 VB-25. The one colour the whole model resolves to when every section is answered, complete and fresh — every orb, every edge and every bloom at once, so the solid reads as one lit object instead of a network of differently-lit parts. Brighter and whiter than any of the five node colours on purpose: the change has to be obvious without being a flourish. It is a STATE and never a reward (docs/GUARDRAILS.md) — losing it says 'something is out of date', never anything about the person." },
      "unified-mid":  { "value": "#63C8F5", "role": "the bloom behind a unified orb — `unified`'s own mid, so the light around an orb is the light in it" },
      "unified-deep": { "value": "#0C2A4E", "role": "what the back of a unified orb sinks toward. The same depth cue every orb already has (`brainglobe-shade`), in the unified colour's own deep — one colour must not mean one flat drawing." },
      "highlight":   { "value": "#EDF3FF", "role": "the specular dot every sphere shares, offset to 34%/30% so all twelve are lit from one place" },
      "field":       { "value": "#060A16", "role": "the deep-field stage the globe sits on" },
      "field-glow":  { "value": "#121B3A", "role": "centre of the stage's radial — the field is not flat, or the globe has nothing to sit in front of" },
      "edge":        { "value": "#7396E4", "role": "a near edge of the solid" },
      "edge-far":    { "value": "#2C3B70", "role": "a far edge — depth is carried by colour as well as width and opacity" },
      "label":       { "value": "#E8EDF9", "contrast": 16.86, "level": "AAA", "role": "section labels on `field`. Depth dims them to 0.72 opacity at the very back, which still measures 8.81:1 — far must read as far without dropping below 4.5:1." },
      "focus":       { "value": "#A8C7FF", "contrast": 11.55, "level": "AAA", "role": "the focus ring inside the stage. --primary is the panel's ring everywhere else and measures 2.90:1 on this field — under the 3:1 floor for a non-text indicator — so the dark stage gets an equal-or-better replacement rather than a ring nobody can find (docs/GUARDRAILS.md)." },
      "ring":        { "value": "#7BE0B4", "contrast": 12.39, "level": "AAA", "role": "the halo on the section being written now. Never the only signal — the halo is a shape, and the state is in the node's accessible name too." },
      "panel":       { "value": "#141F44", "role": "V1.4 VB-23. The surface the sub-section detail panel sits on when the stage splits. 1.23:1 on `field` — deliberately near-invisible on its own, because it is a *place* rather than a card, and the hairline below is what actually draws its edge. Non-interactive, so no 3:1 boundary is claimed for it." },
      "panel-line":  { "value": "#35478A", "role": "the detail panel's hairline. 1.85:1 on `panel` — a decorative separator, the same job `divider` does on the light canvas, and never a control boundary." },
      "detail-key":  { "value": "#A6B6DC", "contrast": 7.90, "level": "AAA", "role": "the question a detail cell answers, on `panel` (9.74:1 on `field` where the panel is see-through). Quieter than `label`'s 13.68:1 so the person's own words are the loud thing in the cell — quieter, never under 4.5:1." }
    }
  },
  "font": {
    "sans": { "value": "-apple-system, BlinkMacSystemFont, 'Segoe UI', Inter, Roboto, 'Helvetica Neue', Arial, sans-serif" },
    "mono": { "value": "ui-monospace, SFMono-Regular, 'SF Mono', Menlo, Consolas, 'Liberation Mono', monospace" }
  },
  "type": {
    "display":  { "size": "32px", "line": 1.2,  "weight": 680, "tracking": "-0.02em",  "use": "site only, never the panel" },
    "wordmark": { "size": "26px", "line": 1.1,  "weight": 680, "tracking": "-0.022em", "use": "the 'Workbrain' wordmark on the welcome screen, and nothing else. Deliberately smaller than type.display, which is site-only — 32px is too loud at 400px wide." },
    "question": { "size": "22px", "line": 1.25, "weight": 660, "tracking": "-0.018em", "use": "the one question on a panel screen" },
    "section":  { "size": "17px", "line": 1.4,  "weight": 650 },
    "body":     { "size": "15px", "line": 1.6,  "weight": 400, "use": "never below 14px anywhere" },
    "support":  { "size": "13px", "line": 1.5,  "weight": 400 },
    "label":    { "size": "12px", "line": 1.4,  "weight": 650, "tracking": "0.08em", "transform": "uppercase", "use": "2–3 words max" }
  },
  "space": { "scale": [4, 8, 12, 16, 20, 24, 32, 48, 64], "base": 4 },
  "radius": { "sm": "6px", "md": "10px", "lg": "14px", "pill": "999px" },
  "elevation": {
    "1": { "value": "0 1px 2px rgba(21,24,29,.06)",  "use": "hover lift on an interactive row" },
    "2": { "value": "0 4px 12px rgba(21,24,29,.08), 0 1px 3px rgba(21,24,29,.06)", "use": "the panel against the page" },
    "3": { "value": "0 16px 40px rgba(21,24,29,.14), 0 2px 8px rgba(21,24,29,.08)", "use": "sheets and toasts only" }
  },
  "motion": {
    "fast": "120ms", "base": "200ms", "slow": "320ms",
    "ease": "cubic-bezier(.2,0,0,1)",
    "$comment": "One easing curve for everything. Three durations. No others."
  },
  "target": { "min": "44px", "$comment": "every control, including icon buttons" },
  "focus":  { "ring": "2px solid var(--primary)", "offset": "2px" }
}
